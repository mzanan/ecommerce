'use client'

import { AdminLoginForm } from "@/components/admin/auth/LoginForm/LoginForm"

export default function LoginPage() {
  return (
    <main className="container">
      <AdminLoginForm />
    </main>
  )
}